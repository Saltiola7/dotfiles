# Added by Colima
{{ if stat (joinPath .chezmoi.homeDir ".colima/ssh_config") -}}
Include {{ .chezmoi.homeDir }}/.colima/ssh_config
{{ end }}
# Added by OrbStack: 'orb' SSH host for Linux machines
# This only works if it's at the top of ssh_config (before any Host blocks).
# This won't be added again if you remove it.
Include {{ .chezmoi.homeDir }}/.orbstack/ssh/config

# Mac mini (Tailscale) — herdr remote target: `herdr --remote mac-mini`
Host mac-mini
  HostName 100.73.117.118
  User tis

Host *
  IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
  StrictHostKeyChecking ask
  HashKnownHosts yes
  ServerAliveInterval 60
  ServerAliveCountMax 3
